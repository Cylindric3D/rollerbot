# Don't check in GCode - it's machine-dependent
*.gcode

# For now, don't check in the PNG files, these can be re-generated by OpenSCAD
*.png

# For now, don't check in the STL files, these can be re-generated by OpenSCAD
*.stl

# Don't check in various temp, backup and Windows-related files
desktop.ini
*.bak
*~
